import {Survey, Question, Answer, User} from "../../../db/models"

export default async function handler(req, res){

    const method = req.method

    switch (method) {
        case "GET":
            // 
            break;
        case "POST":

            break;
        default:
            break;
    }

}